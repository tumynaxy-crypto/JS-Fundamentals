// Get the arguments passed to the script (excluding 'node' and the script name)
const args = process.argv.slice(2);

// Print the first and second arguments in the required format
console.log(args[0] + " is " + args[1]);
x
